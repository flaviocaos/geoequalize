PySide6>=6.6.0
rasterio>=1.3.9
# GDAL>=3.7.0  # NAO INSTALAR: o rasterio ja inclui os binarios do GDAL.
# O pacote "GDAL" standalone exige compilar C++ e trava no Windows sem o
# Visual C++ Build Tools. Como nosso codigo so usa rasterio (nunca importa
# "osgeo" diretamente), essa linha pode ficar comentada com seguranca.
numpy>=1.26.0
scipy>=1.11.0
opencv-python>=4.9.0
scikit-image>=0.22.0
matplotlib>=3.8.0
pyqtgraph>=0.13.3
dask[array]>=2024.1.0
pytest>=8.0.0
reportlab>=4.0.0
scikit-learn>=1.4.0

# Nota sobre GDAL no Windows:
# instalar o GDAL via pip puro costuma falhar por falta de binarios compilados.
# Recomenda-se usar:
#   conda install -c conda-forge gdal rasterio
# ou baixar wheels pre-compilados de:
#   https://github.com/cgohlke/geospatial-wheels
# (rasterio ja traz binarios do GDAL embutidos na maioria dos casos)